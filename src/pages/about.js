import * as React from "react";
import Layout from "../components/layout";

const AboutPage = () => {
  return (
    <Layout pageTitle="About Me">
      <p>This is another page using the Layout component</p>
    </Layout>
  );
};

export default AboutPage;
